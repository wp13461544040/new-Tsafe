# 🔴 Shell 脚本必须是 LF。
#    Windows 上 git 默认 `core.autocrlf=true` 会在 checkout 时转成 CRLF，
#    而 Linux 的 `#!/usr/bin/env bash` 遇到 `\r` 会报
#    `bad interpreter: No such file or directory` —— 错误信息完全指不出真因。
*.sh text eol=lf

# 同理：容器内执行的配置文件
Dockerfile text eol=lf
*.conf text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
